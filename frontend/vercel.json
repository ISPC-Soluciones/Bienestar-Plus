{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "buildCommand": "npm install && npx ng build --configuration production",
  "outputDirectory": "dist/mi-proyecto",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
