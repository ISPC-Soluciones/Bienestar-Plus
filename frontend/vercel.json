{
  "builds": [{ "src": "package.json", "use": "@vercel/static-build" }],
  "buildCommand": "npx @angular/cli build --configuration production",
  "outputDirectory": "dist/mi-proyecto/browser",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
