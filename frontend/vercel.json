{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "buildCommand": "cd frontend && npx ng build --configuration production",
  "outputDirectory": "frontend/dist/mi-proyecto",
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
